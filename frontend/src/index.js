import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css'; // Menghubungkan file CSS untuk styling
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
